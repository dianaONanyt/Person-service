spring:
  application:
    name: Person-service
  datasource:
    url: jdbc:postgresql://postgres:5432/personbd
    username: dianauser
    password: pass
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update   
    show-sql: true       
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
server:
  port: 8081
  message: Soy el programa 2 de personas
person:
  file:
    path: /home/diana_user/datosPerson/people.txt